<UserControl x:Class="AadharLocation.AdminDashboard.Views.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:AadharLocation.AdminDashboard.Converters">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Converters/ConverterResources.xaml"/>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24" MaxWidth="700" HorizontalAlignment="Left">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Page header -->
            <TextBlock Grid.Row="0" Text="Settings" FontSize="24" FontWeight="SemiBold"
                       Foreground="{DynamicResource MaterialDesignBody}" Margin="0,0,0,24"/>

            <!-- Alert Email Recipients card -->
            <Border Grid.Row="1" CornerRadius="8" Padding="24" Margin="0,0,0,16"
                    Background="{DynamicResource MaterialDesignPaper}">
                <StackPanel>
                    <TextBlock Text="Alert Email Recipients" FontSize="16" FontWeight="SemiBold"
                               Foreground="{DynamicResource MaterialDesignBody}" Margin="0,0,0,4"/>
                    <TextBlock Text="These addresses receive all alert emails alongside admin users."
                               Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,16"/>

                    <!-- Input row -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 materialDesign:HintAssist.Hint="Enter email address and press Enter or click Add"
                                 materialDesign:HintAssist.Foreground="{DynamicResource MaterialDesignBodyLight}"
                                 Foreground="{DynamicResource MaterialDesignBody}"
                                 CaretBrush="{DynamicResource MaterialDesignBody}"
                                 Text="{Binding NewRecipientEmail, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Return" Command="{Binding AddRecipientCommand}"/>
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Grid.Column="2" Content="Add"
                                Command="{Binding AddRecipientCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Padding="16,0" VerticalAlignment="Bottom"/>
                    </Grid>

                    <TextBlock Text="{Binding RecipientStatusMessage}"
                               Margin="0,0,0,8" FontSize="12"
                               Foreground="#EF4444"
                               Visibility="{Binding RecipientStatusMessage, Converter={StaticResource StringNotEmptyToVisibility}}"/>

                    <!-- Chips list below input -->
                    <ItemsControl ItemsSource="{Binding AlertEmailRecipients}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12"
                                        Background="#1F2DD4BF"
                                        BorderBrush="#2DD4BF"
                                        BorderThickness="1"
                                        Margin="0,4,6,0" Padding="10,4,2,4">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding}"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Foreground="#2DD4BF"
                                                   Margin="0,0,2,0"/>
                                        <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                Padding="0" Width="20" Height="20"
                                                MinWidth="0" MinHeight="0"
                                                ToolTip="Remove"
                                                Foreground="#2DD4BF"
                                                Command="{Binding DataContext.RemoveRecipientCommand,
                                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}">
                                            <materialDesign:PackIcon Kind="Close" Width="12" Height="12"/>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Save row -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center"
                           Foreground="#2DD4BF"
                           Visibility="{Binding StatusMessage, Converter={StaticResource StringNotEmptyToVisibility}}"/>
                <Button Grid.Column="1" Content="Save Settings"
                        Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButtonStyle}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
