<Window x:Class="AadharLocation.AdminDashboard.Views.Dialogs.ConfirmDeleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Confirm Delete"
        Width="380" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource CardBackgroundBrush}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="28" TextElement.Foreground="{DynamicResource PrimaryTextBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <materialDesign:PackIcon Kind="AlertCircleOutline" Width="24" Height="24"
                                     Foreground="#F87171" VerticalAlignment="Center"
                                     Margin="0,0,10,0"/>
            <TextBlock Text="Confirm Delete"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Rectangle Grid.Row="1" Height="1" Fill="{DynamicResource SeparatorBrush}" Margin="0,0,0,20"/>

        <TextBlock x:Name="MessageText"
                   Grid.Row="2"
                   FontSize="13"
                   Foreground="{DynamicResource PrimaryTextBrush}"
                   TextWrapping="Wrap"
                   VerticalAlignment="Center"/>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="No"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="NoButton_Click"/>
            <Button Grid.Column="3" Content="Yes, Delete"
                    Style="{StaticResource DangerButtonStyle}"
                    Click="YesButton_Click"/>
        </Grid>
    </Grid>
</Window>
