<Window x:Class="AadharLocation.AdminDashboard.Views.Dialogs.UninstallCodeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Uninstall Code Generated"
        Width="380" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource CardBackgroundBrush}"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Buttons.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="28" TextElement.Foreground="{DynamicResource PrimaryTextBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon + Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <materialDesign:PackIcon Kind="Key" Width="24" Height="24"
                                     Foreground="#0D9488" VerticalAlignment="Center"
                                     Margin="0,0,10,0"/>
            <TextBlock Text="Uninstall Code Generated"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryTextBrush}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <Rectangle Grid.Row="1" Height="1" Fill="{DynamicResource SeparatorBrush}" Margin="0,0,0,20"/>

        <!-- Code display + helper -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center">
            <TextBlock Text="Share this code with the operator:"
                       FontSize="12" Foreground="{DynamicResource SecondaryTextBrush}"
                       Margin="0,0,0,12"/>

            <!-- Code box -->
            <Border Background="{DynamicResource MaterialDesignChipBackground}"
                    CornerRadius="8" Padding="16,12"
                    HorizontalAlignment="Center">
                <TextBlock x:Name="CodeText"
                           FontSize="32" FontWeight="Bold"
                           FontFamily="Consolas"
                           Foreground="#0D9488"
                           HorizontalAlignment="Center"/>
            </Border>

            <TextBlock Text="This code can only be used once and will be cleared after the operator uninstalls."
                       FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}"
                       TextWrapping="Wrap" TextAlignment="Center"
                       Margin="0,12,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Copy Code"
                    Click="CopyButton_Click"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
            <Button Grid.Column="3" Content="Close"
                    IsDefault="True"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="CloseButton_Click"/>
        </Grid>
    </Grid>
</Window>
